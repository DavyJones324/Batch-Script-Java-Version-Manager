# Batch-Script-Java-Version-Manager
This QOL program is meant to eliminate Java's trace amounts of backward incompatibility by switching between different installed versions. It is not required for running older Java programs, but it is both optional and complementary.
